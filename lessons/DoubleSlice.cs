using System;
using System.Collections.Generic;
using static System.Math;

class Solution 
{
    // 0 ≤ X < Y < Z < N doublie size
    // sum is sum - vertices A[X], A[Y], A[Z]
    // -10,000 - 10,000
    public int solution(int[] A) 
    {
        int max = 0;
        
        // find 2 minimums
        
        
        
        // remove largest Y inside the slice
    }
}